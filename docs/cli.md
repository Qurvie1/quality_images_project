# Command line interface

The CLI is implemented with Click and exposed through the `iqsort` script in
`pyproject.toml`.

```{eval-rst}
.. click:: image_quality_sorter.cli:main
   :prog: iqsort
   :nested: full
```
